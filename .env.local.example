# Copie para .env.local (nunca commitar .env.local)
# Obtenha em: Supabase → Project Settings → API

NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua_chave_anon_aqui

# NÃO adicionar SUPABASE_SERVICE_ROLE_KEY aqui nem no frontend/Vercel público.
# A service_role bypassa RLS e deve ficar apenas em scripts server-side isolados, se um dia forem necessários.